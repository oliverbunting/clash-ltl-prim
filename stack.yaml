resolver: lts-18.1

extra-deps:
  # At the time of writing, no snapshot includes Clash 1.4 yet so we add it - and
  # its dependencies - manually.
  - lazysmallcheck-0.6
  - Stream-0.4.7.2
  - arrows-0.4.4.2
  - clash-prelude-1.4.2
  - clash-lib-1.4.2
  - clash-ghc-1.4.2
  # Required for liquid haskell
  - liquid-base-4.14.1.0
  - liquidhaskell-0.8.10.2
  - liquid-ghc-prim-0.7.0
  - Diff-0.3.4

# Needed due to some liquid haskell dependencies
allow-newer: true
